{
  "name": "lycee-terminal-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Reference WebSocket gateway giving each student a disposable, resource-limited Docker container. NOT part of the Next.js build.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dockerode": "^4.0.2",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.18.0"
  }
}
